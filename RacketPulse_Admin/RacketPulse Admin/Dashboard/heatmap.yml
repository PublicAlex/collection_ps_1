info:
  name: heatmap
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/matches/dashboard/heatmap?startDate=2026-01-01&endDate=2026-12-31&gameTypes=open,private,tournament,training,external,cancelled"
  params:
    - name: startDate
      value: 2026-01-01
      type: query
    - name: endDate
      value: 2026-12-31
      type: query
    - name: gameTypes
      value: open,private,tournament,training,external,cancelled
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
