info:
  name: chart
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/matches/dashboard/chart?startDate=2026-02-10&endDate=2026-03-10&groupBy=day&gameTypes=open,private,tournament&playerLevelFrom=0.0&playerLevelTo=7.0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
