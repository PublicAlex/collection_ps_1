info:
  name: players stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/matches/dashboard/player-levels?startDate=2026-01-01&endDate=2026-12-31"
  params:
    - name: startDate
      value: 2026-01-01
      type: query
    - name: endDate
      value: 2026-12-31
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
