info:
  name: Delete User (Soft)
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/users/:id"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  params:
    - name: id
      value: "2"
      type: path
      description: ID of the user to delete
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
