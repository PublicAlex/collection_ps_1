meta {
  name: Update User
  type: http
}

patch {
  url: {{base_url}}/users/:id
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
      "name": "Updated Name"
  }
}
