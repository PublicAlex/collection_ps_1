meta {
  name: set recovery option phone verify
  type: http
}

post {
  url: {{host}}/club_users/:id/recovery-options/phone/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  
}
