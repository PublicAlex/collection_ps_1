meta {
  name: set recovery option phone
  type: http
}

post {
  url: {{host}}/club_users/:id/recovery-options/phone
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  
}
