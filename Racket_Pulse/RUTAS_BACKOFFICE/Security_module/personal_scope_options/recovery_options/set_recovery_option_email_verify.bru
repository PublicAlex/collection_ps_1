meta {
  name: set recovery option email verify
  type: http
}

post {
  url: {{host}}/club_users/:id/recovery-options/email/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {

}
