meta {
  name: Clone Players from Club to Club
  type: http
}

post {
  url: {{host}}/users/clone-players/from/:sourceClubId/to/:targetClubId
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}
