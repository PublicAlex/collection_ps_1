info:
  name: get user banners
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/banners"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
