info:
  name: get user statistics
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/user_statistics/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
