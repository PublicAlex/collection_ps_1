info:
  name: disable fcm token to user device
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}/device_tokens/users/1/current_device/deactivate"
  body:
    type: json
    data: "{\r

      \  \"token\": \"dGVzdF90b2tlbl9mY2sdfwerXBsZQd12\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
