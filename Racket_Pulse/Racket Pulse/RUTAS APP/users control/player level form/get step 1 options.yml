info:
  name: get step 1 options
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/level_onboarding/step1/options"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
