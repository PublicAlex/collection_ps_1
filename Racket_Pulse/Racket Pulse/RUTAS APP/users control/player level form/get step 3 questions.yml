info:
  name: get step 3 questions
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/level_onboarding/step3/questions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
