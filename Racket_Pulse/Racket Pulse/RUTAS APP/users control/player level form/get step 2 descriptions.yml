info:
  name: get step 2 descriptions
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/level_onboarding/step2/descriptions?level=4"
  params:
    - name: level
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
