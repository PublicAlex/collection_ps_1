info:
  name: get user locations
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/user_locations/user/6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
