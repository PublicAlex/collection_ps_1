info:
  name: get current user location
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/user_locations/user/1/current"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
