info:
  name: get club paypal method of payment
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/club_payment_configs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
