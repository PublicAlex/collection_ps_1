info:
  name: get match (token)
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/matches/20?backoffice=true"
  params:
    - name: sortBy
      value: start_date
      type: query
      description: "Opciones: start_date, end_date, date_created, id.  Por defecto: start_date"
      disabled: true
    - name: backoffice
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
