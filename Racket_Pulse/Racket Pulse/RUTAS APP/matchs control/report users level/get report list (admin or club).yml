info:
  name: get report list (admin or club)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/player_level_reports"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
