info:
  name: get  score vote (token)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/match_score_proposals/match/28"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
