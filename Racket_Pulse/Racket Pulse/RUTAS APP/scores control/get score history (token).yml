info:
  name: get score history (token)
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/score_history/match/27"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
