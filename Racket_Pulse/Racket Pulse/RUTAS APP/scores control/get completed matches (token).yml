info:
  name: get completed matches (token)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/matches/user/1/completed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
