info:
  name: get courts by club id (token)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/courts/club/2"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
