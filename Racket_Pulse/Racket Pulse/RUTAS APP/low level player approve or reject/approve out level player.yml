info:
  name: approve out level player
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/match_teams/78/approve-player"
  body:
    type: json
    data: "{\r

      \"voting_player_id\":3, // jugador que está votando \r

      \"player_id\":2 // jugador que se encuentra en estado pendiente\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
