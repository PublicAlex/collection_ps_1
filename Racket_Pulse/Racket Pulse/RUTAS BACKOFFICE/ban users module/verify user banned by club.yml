info:
  name: verify user banned by club
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/club_user_bans/check/5/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
