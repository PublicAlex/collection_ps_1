info:
  name: cancel match (token)
  type: http
  seq: 10

http:
  method: PUT
  url: "{{host}}/matches/team/511"
  body:
    type: json
    data: "{\r\n  \"status\": \"cancelled\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
