info:
  name: get match (token)
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/matches/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
