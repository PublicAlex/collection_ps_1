info:
  name: get all matches (admin)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/matches"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
