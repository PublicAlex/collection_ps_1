info:
  name: get event
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/events/3"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Generated from cURL: curl --location '{{host}}/events/1' \\\r

  --header 'Accept: application/json' \\\r

  --header 'Content-Type: application/json'\r

  # Uses: Get event details, includes relations (club, creator, event_users)"
