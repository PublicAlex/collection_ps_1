info:
  name: get club user relations (token)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/club_gestor/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
