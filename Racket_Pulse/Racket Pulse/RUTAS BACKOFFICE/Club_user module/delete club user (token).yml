info:
  name: delete club user (token)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{host}}/club_users/5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
