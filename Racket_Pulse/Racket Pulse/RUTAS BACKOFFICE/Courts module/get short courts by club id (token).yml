info:
  name: get short courts by club id (token)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/courts/club/2?status=enabled"
  params:
    - name: backoffice
      value: "true"
      type: query
      disabled: true
    - name: status
      value: enabled
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
