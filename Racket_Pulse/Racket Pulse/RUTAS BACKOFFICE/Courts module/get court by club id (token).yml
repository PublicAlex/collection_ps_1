info:
  name: get court by club id (token)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/courts/44/with-prices"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
