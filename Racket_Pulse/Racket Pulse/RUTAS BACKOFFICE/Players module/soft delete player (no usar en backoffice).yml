info:
  name: soft delete player (no usar en backoffice)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/users/{id}/soft"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
