info:
  name: get organization
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/organizations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
