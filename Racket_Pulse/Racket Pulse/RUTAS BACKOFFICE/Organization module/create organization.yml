info:
  name: create organization
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/organizations"
  body:
    type: json
    data: "{\r

      \  \"name\": \"Padel Organization\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
