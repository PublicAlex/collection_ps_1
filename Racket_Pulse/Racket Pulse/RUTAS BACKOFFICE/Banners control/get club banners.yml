info:
  name: get club banners
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/clubs/1/banners"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
