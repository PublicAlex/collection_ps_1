info:
  name: get country list
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/country_clubs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
