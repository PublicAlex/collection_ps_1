info:
  name: get VAT country regex
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/country_clubs/vat_regex/IDN"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
