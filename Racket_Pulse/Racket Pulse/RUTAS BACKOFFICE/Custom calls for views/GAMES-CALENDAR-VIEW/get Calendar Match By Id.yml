info:
  name: get Calendar Match By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/matches/calendar/match/160"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
