info:
  name: get players stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/users/club/stats?clubId=1"
  params:
    - name: clubId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
