info:
  name: complete backoffice to do item Copy
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}/tasks/setup_progress/:clubId:stepId/complete"
  params:
    - name: clubId:stepId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
