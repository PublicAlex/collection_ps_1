info:
  name: get club billing plan status
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/billing-plans/club/27/status"
  params:
    - name: active
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
