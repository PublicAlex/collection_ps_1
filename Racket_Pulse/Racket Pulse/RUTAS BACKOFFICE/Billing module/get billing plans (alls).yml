info:
  name: get billing plans (alls)
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/billing-plans"
  params:
    - name: active
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
