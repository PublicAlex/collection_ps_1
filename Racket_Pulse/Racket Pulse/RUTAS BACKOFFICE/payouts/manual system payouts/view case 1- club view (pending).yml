info:
  name: "view case 1: club view (pending)"
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/clubs/56/scheduled-payouts?limit=10"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
