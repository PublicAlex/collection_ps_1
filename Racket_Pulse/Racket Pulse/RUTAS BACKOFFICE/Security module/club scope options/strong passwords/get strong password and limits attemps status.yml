info:
  name: get strong password and limits attemps status
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/club_users/1/security-settings"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
