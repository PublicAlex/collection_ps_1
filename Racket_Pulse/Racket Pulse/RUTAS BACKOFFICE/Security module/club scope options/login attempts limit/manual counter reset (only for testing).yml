info:
  name: manual counter reset (only for testing)
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/club_users/1/security-settings/unlock"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
