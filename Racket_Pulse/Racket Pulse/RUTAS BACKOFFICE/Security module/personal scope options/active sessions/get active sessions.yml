info:
  name: get active sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/club_users/:id/sessions"
  params:
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
