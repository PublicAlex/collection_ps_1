info:
  name: revoke all active sessions of user
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/club_users/:id/sessions/revoke-all"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
