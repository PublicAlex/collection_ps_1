info:
  name: revoke active session
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/club_users/:id/sessions/revoke"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
