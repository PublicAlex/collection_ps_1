info:
  name: set recovery option email
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/club_users/:id/recovery-options/email"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
