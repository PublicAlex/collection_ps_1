info:
  name: get recovery options
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/club_users/1/recovery-options"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
