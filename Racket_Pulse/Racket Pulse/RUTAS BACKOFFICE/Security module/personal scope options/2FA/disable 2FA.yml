info:
  name: disable 2FA
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/club_users/:id/2fa/disable"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: "{\r\n  \"token\": \"123456\"\r\n}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
