info:
  name: verify status 2FA
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/club_users/1/2fa/status"
  body:
    type: json
    data: "{\r\n  \"token\": \"765929\"\r\n}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
