info:
  name: regenerate backups code
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/club_users/1/2fa/regenerate-backup-codes"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
