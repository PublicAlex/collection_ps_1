info:
  name: reconnect
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/whatsapp/reconnect"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
