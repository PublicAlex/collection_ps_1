info:
  name: get qr status
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/whatsapp/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
